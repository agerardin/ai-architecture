version: '3.8'
services:
  redis:
    image: redis:8.2.1-bookworm
    ports:
      - "63379:6379"
    volumes:
      - redis_data_test:/data
    healthcheck:
      test: ["CMD-SHELL", "redis-cli ping"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - shared_net

volumes:
  redis_data_test:
    name: redis_data_test
    
networks:
  shared_net:
    external: true
